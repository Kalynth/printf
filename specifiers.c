#include "main.h"

/**
 * handle_char - function to print a char
 * @lol: Arguments
 * Return: integer
 */
int handle_char(va_list lol)
{
	char k;

	k = va_arg(lol, int);
	_putchar(str);
	return (1);
}
